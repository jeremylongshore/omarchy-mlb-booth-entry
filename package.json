{
  "name": "omarchy-mlb-booth",
  "version": "1.0.0",
  "private": true,
  "description": "MLB Booth widget for the Omarchy bar - dev harness for the Model.js data layer",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
